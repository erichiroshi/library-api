eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false   # não se registra em si mesmo
    fetch-registry: false
  server:
    wait-time-in-ms-when-sync-empty: 0   # startup mais rápido em dev